# Convenience file for LOCAL development: installs backend + frontend together.
#     pip install -r requirements.txt
# Deployments use the split files instead:
#     requirements-backend.txt   -> the API (needs ~2 GB RAM because of PyTorch/spaCy models)
#     frontend/requirements.txt  -> the Streamlit UI (small)
-r requirements-backend.txt
-r frontend/requirements.txt
