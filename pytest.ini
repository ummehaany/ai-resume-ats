[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: talks to real external services (Groq / Supabase); skipped unless RUN_INTEGRATION=1
filterwarnings =
    ignore::DeprecationWarning
